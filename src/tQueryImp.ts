import {tQuery} from "./tQuery";

class tQueryImp implements tQuery {
    context: any;
    length: number;

    data(key, value) {
    }

    each(callback: (el: any) => {}): void {
    }

    get(index: number) {
    }

    index(selector: object) {
    }

    queue(e, q) {
    }

    removeData() {
    }

    size(): number {
        return 0;
    }
}
